<% if user_signed_in? %>
	<%= link_to 'Edit Profile', edit_user_path(@user) %>
	
	<!-- Add New Status Form-->
	<%= link_to 'Add New Status','#',:onclick => "$('#new_post_form').slideDown()"%>
	<%= render 'microposts/form'%>

	<%= link_to 'New Album', new_album_path %> 
<%end%>