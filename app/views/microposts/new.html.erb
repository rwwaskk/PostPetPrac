<% @user ||= current_user %>

<%= form_for @micropost do |f| %>
 
  <div class="field">
  
    <%= f.text_area :content, placeholder: "enter new status" %>
  </div>
  <%= f.submit "Post", class: "test" %>
<% end %>