<%= form_for(@micropost) do |f| %>
 
  <div class="field">
  
    <%= f.text_field :content, placeholder: "enter new status" %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>